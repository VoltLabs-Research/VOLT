export interface SSHConnection {
    _id: string;
    name: string;
    host: string;
    port: number;
    username: string;
    team: string;
    user: string;
    createdAt: string;
    updatedAt: string;
};
