# Estado por-máquina escrito en runtime (env del stack + bootstrap con credenciales).
# No es fuente: lo regenera AppConfig.ensureStackDefaults / Bootstrap en cada arranque.
app-config.json
.docker-home/
# Repowise
.repowise

# MCP and Claude
.mcp.json
.claude

# Assembled by scripts/build-stack.mjs; packaged as resources/stack
stack-runtime/

# Runtime data of the local stack in dev (userData/local-stack when packaged)
local-stack/
