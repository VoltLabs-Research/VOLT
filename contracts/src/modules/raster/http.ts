// The raster module carries no client-sent request bodies: every endpoint takes
// its inputs entirely from the path (`:teamId`, `:trajectoryId`, `:timestep`,
// `:analysisId`, `:model`). There are therefore no wire input types to declare
// here.

export {};
