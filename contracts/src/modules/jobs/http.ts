// The jobs module carries no client-sent request bodies: both endpoints take
// their inputs entirely from the path (`:teamId`, `:trajectoryId`). There are
// therefore no wire input types to declare here.

export {};
