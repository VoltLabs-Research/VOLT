// Wire request bodies the CLIENT sends to the session module. Every session
// endpoint is driven entirely by the authenticated user id (server-derived),
// the current token (server-derived), path params and query-string — so there
// are no client-supplied JSON bodies to model here.

export {};
