// The daily-activity read endpoint takes no request body — its inputs (`range`,
// `scope`) arrive as query-string values, and `:teamId` is a path param. This
// module therefore has no client-sent body types.

export {};
