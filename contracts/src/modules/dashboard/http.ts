// The dashboard global-search endpoint takes no request body — its inputs
// (`query`, `limit`) arrive as query-string values, and `:teamId` is a path
// param. This module therefore has no client-sent body types.

export {};
