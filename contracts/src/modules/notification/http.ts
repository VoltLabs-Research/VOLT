// Wire request bodies the CLIENT sends to the notification module. Both
// endpoints are driven entirely by the authenticated user id (server-derived)
// and query-string pagination — there are no client-supplied JSON bodies to
// model here.

export {};
