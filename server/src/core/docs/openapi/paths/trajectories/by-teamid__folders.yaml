get:
  operationId: listFolders
  tags:
  - Trajectories
  summary: List folders
  description: Returns all trajectory folders for the specified team.
  parameters:
  - $ref: ../../components/parameters/common.yaml#/TeamId
  - name: parentId
    in: query
    description: Filter by parent folder ID. Omit to list root-level folders.
    schema:
      type: string
  responses:
    '200':
      description: Successful response
      content:
        application/json:
          schema:
            allOf:
            - $ref: ../../components/schemas/common.yaml#/SuccessResponse
            - type: object
              properties:
                data:
                  type: array
                  items:
                    $ref: ../../components/schemas/common.yaml#/Folder
    '400':
      $ref: ../../components/responses/common.yaml#/BadRequest
    '401':
      $ref: ../../components/responses/common.yaml#/Unauthorized
    '403':
      $ref: ../../components/responses/common.yaml#/Forbidden
post:
  operationId: createFolder
  tags:
  - Trajectories
  summary: Create a folder
  description: Creates a new folder for organizing trajectories within the specified team.
  parameters:
  - $ref: ../../components/parameters/common.yaml#/TeamId
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          required:
          - name
          properties:
            name:
              type: string
              description: Name of the folder
            parentId:
              type:
              - string
              - 'null'
              description: ID of the parent folder, or null for root-level
  responses:
    '201':
      description: Folder created successfully
      content:
        application/json:
          schema:
            allOf:
            - $ref: ../../components/schemas/common.yaml#/SuccessResponse
            - type: object
              properties:
                data:
                  $ref: ../../components/schemas/common.yaml#/Folder
    '400':
      $ref: ../../components/responses/common.yaml#/BadRequest
    '401':
      $ref: ../../components/responses/common.yaml#/Unauthorized
    '403':
      $ref: ../../components/responses/common.yaml#/Forbidden
