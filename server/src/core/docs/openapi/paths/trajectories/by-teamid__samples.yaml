get:
  operationId: listSamples
  tags:
  - Trajectories
  summary: List sample trajectory files
  description: Returns a list of available sample trajectory files that can be downloaded for testing and demonstration purposes.
  parameters:
  - $ref: ../../components/parameters/common.yaml#/TeamId
  responses:
    '200':
      description: Successful response
      content:
        application/json:
          schema:
            allOf:
            - $ref: ../../components/schemas/common.yaml#/SuccessResponse
            - type: object
              properties:
                data:
                  type: array
                  items:
                    type: object
                    properties:
                      filename:
                        type: string
                        description: Name of the sample file
                      description:
                        type: string
                        description: Brief description of the sample trajectory
                      fileSize:
                        type: integer
                        format: int64
                        description: File size in bytes
                      format:
                        type: string
                        description: Trajectory file format
    '400':
      $ref: ../../components/responses/common.yaml#/BadRequest
    '401':
      $ref: ../../components/responses/common.yaml#/Unauthorized
    '403':
      $ref: ../../components/responses/common.yaml#/Forbidden
