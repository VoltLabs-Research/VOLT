post:
  operationId: commitTrajectoryUploadSession
  tags:
  - Trajectories
  summary: Commit trajectory upload session
  description: Verifies uploaded objects, composes chunked uploads when needed, and starts trajectory ingestion on the cluster.
  parameters:
  - $ref: ../../components/parameters/common.yaml#/TeamId
  - name: uploadSessionId
    in: path
    required: true
    schema:
      type: string
  responses:
    '200':
      description: Upload session committed
      content:
        application/json:
          schema:
            allOf:
            - $ref: ../../components/schemas/common.yaml#/SuccessResponse
            - type: object
              properties:
                data:
                  $ref: ../../components/schemas/trajectories.yaml#/CommitTrajectoryUploadSessionResponse
    '400':
      $ref: ../../components/responses/common.yaml#/BadRequest
    '401':
      $ref: ../../components/responses/common.yaml#/Unauthorized
    '403':
      $ref: ../../components/responses/common.yaml#/Forbidden
    '404':
      $ref: ../../components/responses/common.yaml#/NotFound
