get:
  tags:
  - Container VNC
  summary: Get VNC connect client script
  description: Retrieve the JavaScript client script used to establish VNC connections.
  operationId: getVncConnectClientScript
  responses:
    '200':
      description: VNC connect client JavaScript file
      content:
        application/javascript:
          schema:
            type: string
