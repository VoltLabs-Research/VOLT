Session:
  type: object
  properties:
    _id:
      type: string
    userAgent:
      type: string
    ip:
      type: string
    lastActivity:
      type: string
      format: date-time
    createdAt:
      type: string
      format: date-time
