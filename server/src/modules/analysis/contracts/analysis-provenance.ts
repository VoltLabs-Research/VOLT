export type AnalysisProvenanceParameters = Record<string, unknown>;

export interface AnalysisProvenanceInputFrameMetadata {
    atomCount: number;
    frameIndex: number;
    trajectoryId: string;
}
